import { PlaygroundShell } from "@/playground/PlaygroundShell";

export const metadata = { title: "TechUI — Component Playground" };

export default function PlaygroundPage() {
  return <PlaygroundShell />;
}
